PORT=5000
HOST=0.0.0.0
# Mongo connection string (e.g., mongodb://localhost:27017/kamrah)
MONGO_URI=

# Cryptographically secure secret for signing JWTs
JWT_SECRET=change-me

# Comma-separated list of allowed web / mobile origins
# Examples: http://localhost:5173,http://127.0.0.1:5173,exp://127.0.0.1:8081
CLIENT_ORIGINS=http://localhost:5173

# Relative path for the WebSocket endpoint that clients will connect to
WS_PATH=/ws/events

# Toggle authentication requirements for local development
# Set to "false" ONLY when you need to run the API without JWT auth (never in production)
REQUIRE_AUTH=true

# Optional: customize log detail
NODE_ENV=development
